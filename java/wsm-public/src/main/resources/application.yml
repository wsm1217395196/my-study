#端口号
server:
  port: 8001

eureka:
  instance:
    hostname: localhost #地址
  client:
    service-url:
#      向注册中心注册服务
      defaultZone: http://wsm:1@${eureka.instance.hostname}:8000/eureka

spring:
  application:
    name: wsm-public #服务名

  mail: #配置邮件
    host: smtp.qq.com
    port: 465
    username: 358632813@qq.com
    password: srudivdfeslkcaic
    properties:
      mail:
        smtp:
          ssl:
            enable: true
  cloud:
    config:
      label: master #指明远程仓库的分支
      profile: java #dev开发环境配置文件,test测试环境,pro正式环境
      uri: http://localhost:8004/ #配置服务中心的网址
      discovery:
        enabled: true #是从配置中心读取文件。
        service-id: wsm-config-server #服务名

