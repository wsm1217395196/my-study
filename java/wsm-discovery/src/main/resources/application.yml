#端口号
server:
  port: 8000

eureka:
  instance:
    hostname: localhost #地址
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka

spring:
  application:
    name: wsm-discovery #服务名





